apiVersion: v1
kind: Service
metadata:
    name: xentask-forms-svc
    namespace: live-prod
spec:
    selector:
      app: xentask-forms
    ports:
     - name: xentask-forms
       protocol: TCP
       port: 8080
       targetPort: 8080
