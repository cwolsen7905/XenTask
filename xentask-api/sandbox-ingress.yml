apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: ws-sandbox
  name: xentask-api-ingress
  annotations:
    field.cattle.io/creatorId: cwolsen
    nginx.ingress.kubernetes.io/proxy-body-size: 256m
spec:
  ingressClassName: nginx
  rules:
  - host: xentask-api.sandbox.your-domain.com
    http:
      paths:
      - backend:
          service:
             name: xentask-api
             port:
               number: 8080
        path: /
        pathType: ImplementationSpecific
  tls:
    - hosts:
      - xentask-api.sandbox.your-domain.com
      secretName: tls-wildcard-your-domain-sandbox
status:
  loadBalancer:
    ingress: